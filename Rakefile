require "rake"
require_relative 'config/application'
require_relative 'config/db'
require_relative 'config/helpers'
dir=File.expand_path(File.dirname(__FILE__))
Dir["#{dir}/tasks/*.rake"].each {|file| load file}